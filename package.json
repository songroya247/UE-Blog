{
  "name": "my-cloudflare-blog",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "node scripts/publish-queue.js && node scripts/generate-llms-txt.js",
    "check-links": "node scripts/check-links.js",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "3.6.0",
    "astro": "^4.0.0"
  }
}
